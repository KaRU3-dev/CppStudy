#include "includes/Functions.h"

namespace CppStudy
{
    // テンプレートを使った関数の定義はヘッダーファイル内に記述する必要があるため、ここではコメントアウトしています。
    // template <typename T>
    // T whichIsGreater(T a, T b)
    // {
    //     return (a > b) ? a : b;
    // }
}
